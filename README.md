# Square Noise

Next.js playground for shuffling uploaded photos or your live webcam feed into glitchy tile grids.

## Getting Started

1. Install dependencies: `npm install`
2. Run the dev server: `npm run dev`
3. Open `http://localhost:3000`

Both modes run completely in the browser—no backend required.
